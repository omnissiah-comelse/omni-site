name: Deploy to Coolify

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify Deploy
        run: |
          curl -sf "http://138.199.233.0:8000/api/v1/deploy?uuid=jksgk8048okoscokgws04844&force=true" \
            -H "Authorization: Bearer ${{ secrets.COOLIFY_API_TOKEN }}" \
            -H "Accept: application/json"
          
          echo "Deploy triggered for omni-site"
